# Docker own files
Dockerfile
docker-compose.yml
.dockerignore

# Git
.git
.gitignore

# Visual Studio
.vs/
bin/
obj/

# Prevent operator-specific overrides from leaking into the image.
# appsettings.json (safe defaults) IS included intentionally.
appsettings.Production.json
appsettings.Development.json

# Documentation
README.md
