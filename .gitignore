# .NET Build artifacts
bin/
obj/
*.user
*.suo
.vs/

# appsettings.json is now tracked (safe defaults only — no secrets).
# Use appsettings.Development.json for local credential overrides.
appsettings.Development.json

# Prevent accidental re-introduction of the old operator-specific file.
appsettings.Production.json

# Keep the example template
!appsettings.example.json

# Environment files
.env
*.env
docker-compose.override.yml

# OS
.DS_Store
Thumbs.db
